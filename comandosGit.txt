git clone repo

//cd al directorio del repo ej:
cd PunterosEnC-

	git status
	// si hay rojo
	git add .
	git commit -m "mensaje de porque se guarda"
	.... //bucle se realiza varias veces

	git push origin master -f
